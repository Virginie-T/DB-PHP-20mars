<html>
<head>
	<link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
	<title>Stylists</title>
</head>
<body>

	<div class='container'>
		<h1>Welcome to the Hair Salon application</h1>

		<p style="font-size: 12px; color: darkred"><em>(This is the version 2.0 of the assessment :D)</em></p>

		<form action='/add_stylist' method='post'>
			<label style="font-size: 20px" for='name'>Enter the stylist name :</label>
			<input id='name' name='name' type='text'>

			<button type='submit' class='btn btn-success'>Add</button>
		</form>

		{% if stylists is not empty %}
		<h2>Here are all the Stylist listed :</h2>
		<ul>
		{% for stylist in stylists %}
			<li><a href='/stylist_form/{{stylist.getId()}}'>{{ stylist.getName }}</a></li>
				   <form style="float: left" action="/stylist_form/{{ stylist.getId }}/deleteSingleStylist" method="post">
                           
                        <input value='{{ stylist.getId }}' name="stylist_id" id="stylist_id" type="hidden">
                        <input name="_method" type="hidden" value="delete">
                        <button type='submit' class="btn btn-danger">Delete</button>
                    </form>
                        
 					<form style="float: left, margin-bottom: 10px" action="/stylist_form/{{ stylist.getId }}/edit" method="post">
                           
                        <input value='{{ stylist.getId }}' name="stylist_id" id="stylist_id" type="hidden">
                        <button style='margin-left: 7px' type='submit' class="btn btn-warning">Edit</button>
                    </form>
                    <br>
		{% endfor %}

		{% else %}

		<p>Sorry, we have no stylist listed yet ...</p>

		{% endif %}

		<form action='/delete_all_the_stylists' method='post'>
			<button type='submit' class="btn btn-danger">! Delete them all !</button>
		</form>

	</div>

</body>
</html>